{
  "name": "nekolessi.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test scripts/*.test.mjs",
    "verify": "npm run check && npm run test"
  }
}
